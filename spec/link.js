import etched from '../etched/etched.js'

/**
 * An object which represents a etched spec link
 * @module etched/spec/link
 * @type {module:etched}
 * @property {null|string} href
 * @property {undefined|string} integrity
 * @property {null|string} rel
 * @property {undefined|string} type
 */
export default etched
  .with({
    href: null,
    rel: null
  })
