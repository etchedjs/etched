import hint from '../hint.js'

/**
 * An object which represents a etched spec time hint
 * @module etched/spec/hints/time
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'time'
  })
