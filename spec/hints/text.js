import hint from '../hint.js'

/**
 * An object which represents a etched spec text hint
 * @module etched/spec/hints/text
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'text'
  })
