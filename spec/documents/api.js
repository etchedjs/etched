import document from '../document.js'

/**
 * An object which represents a etched spec api document
 * @module etched/spec/documents/api
 * @type {module:etched/spec/document}
 * @property {undefined|Object.<string,module:etched/spec/query>} queries
 */
export default document
  .with()
