import hint from '../hint.js'

/**
 * An object which represents a etched spec string hint
 * @module etched/spec/hints/string
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'string'
  })
