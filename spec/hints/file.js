import hint from '../hint.js'

/**
 * An object which represents a etched spec file hint
 * @module etched/spec/hints/file
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'file'
  })
