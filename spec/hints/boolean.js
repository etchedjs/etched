import hint from '../hint.js'

/**
 * An object which represents a etched spec boolean hint
 * @module etched/spec/hints/boolean
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'boolean'
  })
