import hint from '../hint.js'

/**
 * An object which represents a etched spec email hint
 * @module etched/spec/hints/email
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'email'
  })
