import hint from '../hint.js'

/**
 * An object which represents a etched spec week hint
 * @module etched/spec/hints/week
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'week'
  })
