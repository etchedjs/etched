import hint from '../hint.js'

/**
 * An object which represents a etched spec date hint
 * @module etched/spec/hints/date
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'date'
  })
