import etched from '../etched/etched.js'

/**
 * An object which represents a etched spec script
 * @module etched/spec/script
 * @type {module:etched}
 * @property {null|string} integrity
 * @property {null|string} src
 * @property {undefined|string} type
 */
export default etched
  .with({
    integrity: null,
    src: null
  })
