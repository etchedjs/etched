import hint from '../hint.js'

/**
 * An object which represents a etched spec tel hint
 * @module etched/spec/hints/tel
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'tel'
  })
