import hint from '../hint.js'

/**
 * An object which represents a etched spec item hint
 * @module etched/spec/hints/item
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'item'
  })
