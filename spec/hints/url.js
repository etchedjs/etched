import hint from '../hint.js'

/**
 * An object which represents a etched spec url hint
 * @module etched/spec/hints/url
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'url'
  })
