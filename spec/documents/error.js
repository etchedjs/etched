import document from '../document.js'

/**
 * An object which represents a etched spec error document
 * @module etched/spec/documents/error
 * @type {module:etched/spec/document}
 * @property {null|module:etched/spec/error} error
 */
export default document
  .with({
    error: null
  })
