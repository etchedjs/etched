import document from '../document.js'

/**
 * An object which represents a etched spec collection document
 * @module etched/spec/documents/collection
 * @type {module:etched/spec/document}
 * @property {undefined|Object.<string,module:etched/spec/item>} items
 * @property {undefined|number} limit
 * @property {undefined|number} page
 * @property {undefined|number} size
 */
export default document
  .with()
