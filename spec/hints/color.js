import hint from '../hint.js'

/**
 * An object which represents a etched spec color hint
 * @module etched/spec/hints/color
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'color'
  })
