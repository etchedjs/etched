import hint from '../hint.js'

/**
 * An object which represents a etched spec id hint
 * @module etched/spec/hints/id
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'id'
  })
