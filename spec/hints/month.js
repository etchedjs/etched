import hint from '../hint.js'

/**
 * An object which represents a etched spec month hint
 * @module etched/spec/hints/month
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'month'
  })
