import etched from '../etched/etched.js'

/**
 * An object which represents a etched spec detail
 * @module etched/spec/detail
 * @type {module:etched}
 * @property {null|string} message
 * @property {null|string} name
 */
export default etched
  .with({
    message: null,
    name: null
  })
