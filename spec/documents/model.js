import document from '../document.js'

/**
 * An object which represents a etched spec model document
 * @module etched/spec/documents/model
 * @type {module:etched/spec/document}
 * @property {undefined|Object.<string,module:etched/spec/hint>} hints
 * @property {undefined|string} name
 * @property {undefined|Object.<string,module:etched/spec/query>} queries
 */
export default document
  .with()
