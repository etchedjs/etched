import hint from '../hint.js'

/**
 * An object which represents a etched spec number hint
 * @module etched/spec/hints/number
 * @type {module:etched/spec/hint}
 * @property {string} type
 */
export default hint
  .with({
    type: 'number'
  })
